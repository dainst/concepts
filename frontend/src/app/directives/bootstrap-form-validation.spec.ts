import { BootstrapFormValidation } from './bootstrap-form-validation';

describe('BootstrapFormValidation', () => {
  it('should create an instance', () => {
    const directive = new BootstrapFormValidation();
    expect(directive).toBeTruthy();
  });
});
