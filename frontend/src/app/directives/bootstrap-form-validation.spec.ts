import {BootstrapFormValidationDirective } from './bootstrap-form-validation';

describe('BootstrapFormValidation', () => {
  it('should create an instance', () => {
    const directive = new BootstrapFormValidationDirective();
    expect(directive).toBeTruthy();
  });
});
